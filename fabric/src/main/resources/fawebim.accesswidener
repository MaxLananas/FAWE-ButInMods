accessWidener v2 named

# ServerPlayerGameMode#isDestroyingBlock tells the swing hook whether the player
# is currently mining; a mining swing must not re-trigger a brush (the same
# reason WorldEdit's Fabric adapter widens this field).
accessible field net/minecraft/server/level/ServerPlayerGameMode isDestroyingBlock Z

# The command source behind a command tells a player, a console or an rcon
# connection apart from a command block and a function: the automated ones are
# the sources that refuse success messages. WorldEdit widens this same field.
accessible field net/minecraft/commands/CommandSourceStack source Lnet/minecraft/commands/CommandSource;

# The chunk source's main-thread executor is used when a bulk edit has to hand
# work back to the server thread.
accessible class net/minecraft/server/level/ServerChunkCache$MainThreadExecutor
accessible field net/minecraft/server/level/ServerChunkCache mainThreadProcessor Lnet/minecraft/server/level/ServerChunkCache$MainThreadExecutor;

# LevelChunk#setBlockState rebinds the ticker of a block entity whose block
# only changed state. The bulk write keeps the block entities itself, so it
# needs the same method.
accessible method net/minecraft/world/level/chunk/LevelChunk updateBlockEntityTicker (Lnet/minecraft/world/level/block/entity/BlockEntity;)V

# //regen generates its chunks in a temporary level with the live level's
# generator, as WorldEdit's Fabric world does: a seed given to the command
# goes into the primary level data for the time of the generation, reached
# through the derived data of the other dimensions. WorldEdit widens the same
# two fields.
accessible field net/minecraft/world/level/storage/PrimaryLevelData worldOptions Lnet/minecraft/world/level/levelgen/WorldOptions;
mutable field net/minecraft/world/level/storage/PrimaryLevelData worldOptions Lnet/minecraft/world/level/levelgen/WorldOptions;
accessible field net/minecraft/world/level/storage/DerivedLevelData wrapped Lnet/minecraft/world/level/storage/ServerLevelData;
